import 'package:flutter/cupertino.dart';

class BrokerProfile extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Center(
        child:Text("this is proker profile page")
    );
  }

}